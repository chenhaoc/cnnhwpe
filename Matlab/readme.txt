Please excute 'top.m' to generate test case for HWPE.
NOW the parameters in test case are generated randomly, not real parameters in
a CNN model.
In the present, the height or weight size of convolution result need to be a
multiple of 4.

Both input layer and intra layer are supported.
